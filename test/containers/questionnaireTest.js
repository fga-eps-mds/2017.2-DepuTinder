import { renderComponent, expect } from '../test_helper';
import Questionnaire from '../../src/containers/questionnaire';

describe('Questionnaire', () => {
  let component;
  beforeEach(() => {
    component = renderComponent(Questionnaire);
  });
});
