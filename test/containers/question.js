import { renderComponent, expect } from '../test_helper';
import Question from '../../src/containers/question';

describe('Question', () => {
  let component;
  beforeEach(() => {
    component = renderComponent(Question);
  });
});
