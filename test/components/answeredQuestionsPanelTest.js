import { renderComponent, expect } from '../test_helper';
import AnsweredQuestionsPanel from '../../src/components/answeredQuestionsPanel';

describe('AnsweredQuestionsPanel', () => {
  let component;
  beforeEach(() => {
    component = renderComponent(AnsweredQuestionsPanel);
  });
});
